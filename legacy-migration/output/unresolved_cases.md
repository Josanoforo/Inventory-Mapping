# Unresolved Cases

Cases that could not be routed to a single classification during migration and require explicit human decision before proceeding.

---

## Format

Each entry:

- `signal_id` — legacy card ID
- `reason` — why it could not be resolved automatically
- `options` — candidate routings considered
- `blocking_question` — the specific question that needs human input

---

## Cases

*(Empty — no unresolved cases yet. Populated during migration runs.)*
