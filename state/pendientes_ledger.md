# Pendientes — Ledger

**Generado:** S29, Run 1. Cosechado de `Decision_Log_consolidado.md` (tabla de cierre de S26),
`Decision_Log_update_session27.md`, `Handoff_session27.md`, `Decision_Log_update_session28.md`,
`Handoff_session28.md`.

**Sesión vigente:** S36

**Qué es.** Puente entre el inventario de pendientes (vive en project files) y el estado real
(vive en el repo). Existe porque las dos cosas están en lados distintos y ninguna sesión puede
ver las dos a la vez.

**Regla de uso.** Ninguna decisión se discute hasta que su fila tenga `Estado` resuelto con cita.
Si se discute algo sin eso, el ledger falló y se mata; no se le agregan capas.

**Clases.**
- `hecho` — se cierra verificando el repo. No requiere juicio del operador.
- `decisión` — requiere juicio del operador. La verificación solo la enmarca.
- `parqueo` — no es decidible hoy. Necesita condición explícita de desparqueo, no discusión.
  **Parqueado es reversible** — la fila vuelve a su tabla cuando se cumple la condición de
  desparqueo citada. **Cerrado es definitivo** — no vuelve. Por eso viven en listas distintas
  (`**Parqueados:**` / `**Cerrados:**`) aunque ambas saquen la fila del conteo de abiertos.

**Dónde se responde.** `CC` = Claude Code contra el repo · `DSC` = conversación DSC (project
files, diseño) · `OP` = solo el operador.

**Columna Estado.** Vacía en Run 1 por diseño. La llena Run 2 con cita literal.

---

## A. Nunca verificados contra el repo actual — objetivo de Run 2

| ID | Enunciado | Clase | Pregunta verificable | Dónde | Estado |
|---|---|---|---|---|---|
| P-094 | Métricas de flujo por fase diferidas. Los contadores de los manifests no discriminan | hecho | ¿Qué escribe métricas a disco hoy? Enumerar todo script que persista contadores, con ruta de salida | CC | sigue-abierto — 6 scripts `.py` escriben manifest por-corrida (`eje4_xlsx_to_json_batch.py`, `part4_to_recovery_packets.py`, `bulk_extract.py`, `extraction_prepare.py`, `converter_prepare.py`, `signal_prepare.py`); sin métrica cross-fase, confirma el diagnóstico original ·mov:S34 |
| P-092 | Auditoría de atomicidad de Phase 2 ejecutada solo sobre batch_001 | decisión | Enmarque: ¿cuántas cards existen hoy fuera de batch_001? | CC | sigue-abierto — `signal_converter_manifest.json`: `skeletons_processed: 25` = 100% de `batch_001`; `cards_written: 29`; 0 cards fuera de batch_001. La re-extracción doble + adjudicación respondió gran parte por otra vía; residuo real = atomicidad (P-173) ·mov:S34 |
| P-109 | 186 queries del catálogo del eje 4 sin correr. Desbloqueado en S28 | decisión | Enmarque: ¿existe el catálogo de 186 queries en el repo? Ruta y conteo real | CC | sigue-abierto — el `.xlsx` no existe en `main`/`legacy/*`/`preserve/*`; `working/eje4/` no existe; solo script de pre-proceso + contrato del agente, sin correr. Conteo real no verificable desde el repo ·mov:S34 |
| P-141 | `product_type_unclear` falta en `source_packet.schema.json` y en la prosa de `source_packet_conversion_template.md`, y `vocab_check.py` no puede detectarlo porque con `match: subset` solo reporta valores de más | hecho | ¿En qué archivos aparece `product_type_unclear` hoy, y qué reporta `vocab_check.py` sobre ese campo? | CC | mitad de vocabulario ejecutada en E2 (PR #91, D-247); mitad de schemas pendiente de la vía P-151 vs una-a-una (S6) ·mov:S34 |
| P-142 | La rama de fallo de Phase 1b existe PARCIALMENTE en código desde PR #97 (E5a): `bulk_extract.py` implementa `content_not_captured` y escribe a `rejected_archive_phase1b/` (`REJECTED_ARCHIVE_DIR = BASE / 'rejected_archive_phase1b'`, línea 18; `recovery_class='content_not_captured'`, línea 513; `'destination': 'rejected_archive_phase1b'`, línea 639). De las tres clases del CONTRACT (`content_not_captured` \ | `context_missing` \ | `unclassified`, `agents/codex/phase1b-recovery/CONTRACT.md:21`), el script implementa una; `unclassified` existe como directorio de ruteo. Absorbe a P-143 (duplicado fusionado) | hecho→ejecución | ¿Las clases restantes van al script, a la skill, o se declaran fuera de alcance? Cuelga de P-201 | CC | pendiente de E5 ·mov:S35 |
| P-144 | El enum de `uncertainties` en `source_packet.schema.json` no tiene `methodology_unclear`, `anecdotal_single_source` ni `author_conflict_of_interest_possible` — los tres valores que PR #65 agregó al vocab al cerrar P-129a, o sea que la reconciliación fue parcial. `vocab_check.py` no lo detectó porque no cubre ese schema. Misma familia que P-141 | hecho | ¿Qué schemas cubre `vocab_check.py` hoy, y cuáles declaran enums que no compara? | CC | mitad de vocabulario ejecutada en E2 (PR #91, D-247); mitad de schemas pendiente de la vía P-151 vs una-a-una (S6) ·mov:S34 |
| P-146 | El enum de `uncertainties` de `signal_card.schema.json` (13 valores, líneas 284-297) no declara `methodology_unclear`, `anecdotal_single_source` ni `author_conflict_of_interest_possible` — los mismos tres que faltan en `source_packet.schema.json` (13 valores, líneas 191-203; P-144) y que D-225 agregó al core de `pipeline_vocabulary.yaml` (líneas 153-155) al cerrar P-129a. Tercer lugar con los mismos tres valores; la reconciliación quedó parcial y ningún check lo detectó — `vocab_check.py` no lo reporta porque `uncertainties` declara `match: subset` (`pipeline_vocabulary.yaml:139`), que solo reporta valores de más en el schema frente al vocab, no los que al schema le faltan. `data_extraction_record.schema.json` sí trae los tres (16 valores, líneas 341-356) | hecho | ¿En qué schemas está declarado el enum de `uncertainties`, con qué valores cada uno, y cuáles compara `vocab_check.py`? | CC | mitad de vocabulario ejecutada en E2 (PR #91, D-247); mitad de schemas pendiente de la vía P-151 vs una-a-una (S6) ·mov:S34 |
| P-147 | Cinco casos ORIGEN-VACÍO donde la Signal Card tiene valor que no viajó desde Phase 1 (`state/output/phase1_to_card_field_fate.md`, Anexo B, artefacto en main: `state/output/phase1_to_card_field_fate.md`): SC-R1-014, SC-R1-1181, SC-R1-1182 en `metric_value_raw`; SC-R1-022, SC-R1-1179 en `uncertainties`. Phase 2 produce contenido sin origen trazable en el Extraction Record, sin registro en ningún ADR ni módulo | decisión | ¿Qué campos y qué cards, y qué autoriza a Phase 2 a poblar un campo cuyo origen está vacío? | DSC | artefacto en main — `state/output/phase1_to_card_field_fate.md` (PR #75, `main@a1a4084a`) ·mov:S34 |
| P-148 | La distribución de forma de `time_scope_raw` (VACÍO/ANCLA-ÚNICA/MÚLTIPLES-ANCLAS/MEZCLADO/SIN-ANCLA) se produjo con un clasificador de juicio sobre léxico en prosa, no reproducible: dos corridas del mismo léxico sobre el mismo corpus (`state/output/time_scope_raw_distribution.md`, artefacto en main: `state/output/time_scope_raw_distribution.md`, y `state/output/multiples_anclas_verbatim_check.md`, artefacto en main: `state/output/multiples_anclas_verbatim_check.md`) difieren en cuatro de cinco categorías: ANCLA-ÚNICA 290 vs. 275, MÚLTIPLES-ANCLAS 16 vs. 9, MEZCLADO 469 vs. 489, SIN-ANCLA 45 vs. 47. Solo VACÍO (358/1,178) es citable — la única categoría de límite puramente mecánico (`null`, ausente o `""`). Las cifras no reproducibles alimentaron las reformulaciones de P-126 y P-119 en PR #71 antes de detectarse el problema | hecho | ¿Un clasificador determinístico con léxico cerrado en código reproduce la partición, y cuál es entonces el conteo real de cada categoría? | CC | artefactos en main — `state/output/time_scope_raw_distribution.md` (PR #75) y `state/output/multiples_anclas_verbatim_check.md` (PR #76), `main@a1a4084a` ·mov:S34 |
| P-149 | D-225 quedó ejecutado a medias. PR #65 movió los valores de `uncertainties` en `pipeline_vocabulary.yaml`, y `data_extraction_record.schema.json:340-357` nunca se sincronizó del todo: el schema omite los tres `phase_1_only` del vocab (`source_type_unclear`, `metric_type_unclear`, `snippet_needs_reopen`; `pipeline_vocabulary.yaml:157-160`) — 0 records usan hoy cualquiera de los tres (`state/output/vocab_check_blind_spots.md`, artefacto en main: `state/output/vocab_check_blind_spots.md`). Medido: solo-en-schema = ninguno; los 16 valores que el schema declara están todos dentro de los 19 del vocab (14 `core` + los 2 marcados `phase_2_only` que el schema de Phase 1 igual declara, `metric_unit_unclear` y `platform_scope_unclear`) — la afirmación previa de esta fila de que el schema "todavía lista" esos dos como divergencia, y de que la divergencia **bloquea la re-extracción en curso**, no está respaldada por la medición y se retira. `vocab_check.py` no detecta la omisión de los tres `phase_1_only` porque `uncertainties` usa `match: subset` (`pipeline_vocabulary.yaml:139`), que suprime esa dirección (`missing = set()` cuando `match_mode == "subset"`, `vocab_check.py:204-205`). Esa ceguera es un caso, no una clase: de los 23 campos configurados en `vocab_check.py`, 3 declaran `match: subset` (`pointer_type`, `uncertainties`, `manifest_status`); solo `manifest_status` la comparte (cinco de ocho schemas de manifest declaran menos valores que el vocab, con 0 instancias hoy en la dirección ciega); `pointer_type` está limpio (los tres schemas que lo declaran son subconjuntos exactos del vocab, sin valores del vocab ausentes más allá de lo ya marcado `optional`) | decisión | ¿Qué valores declara cada lado, y qué otros campos con `match: subset` tienen la misma ceguera? | DSC | artefacto en main — `state/output/vocab_check_blind_spots.md` (PR #77, `main@a1a4084a`). Mitad de vocabulario ejecutada en E2 (PR #91, D-247); mitad de schemas pendiente de la vía P-151 vs una-a-una (S6) ·mov:S34 |
| P-150 | PREDICCIÓN pre-registrada de la conversación DSC, fechada hoy, pendiente de verificación cuando la re-extracción termine — no un hallazgo: una fracción grande del 51% fuera de enum en `actor_level` no sería error de extracción sino `bulk_extract.py` implementando una heurística de keyword (`help_center` → `marketplace`) que el `assignment_rule` del vocabulario (`pipeline_vocabulary.yaml:30-39`, "quién habla" → `platform` para `help_center`) ya había superado. Dos codificadores independientes a ciegas leyeron el vocab igual. Condición de verificación: cierre de la re-extracción. Si se confirma, parte de la deuda de 3.2 es divergencia de autoridad (código vs. vocab) y se repara en código, no en el corpus. No modifica ninguna fila existente de `actor_level` | hecho | ¿`bulk_extract.py` implementa esta heurística de keyword, y coincide con la fracción fuera de enum de `actor_level` una vez cierre la re-extracción? | CC | lectura de código confirma el mapeo `help_center`→`marketplace` (`bulk_extract.py:167-168`) contra `platform` del vocab (`pipeline_vocabulary.yaml:32`) — y encuentra un mecanismo más amplio no anticipado: `infer_actor_level` resuelve buyer/seller por regex sobre el snippet ANTES de consultar `source_type` (`bulk_extract.py:157-163`), contradiciendo "Determined by source_type, not by topic" (`pipeline_vocabulary.yaml:30-31`). Refuta la mitad explicativa de la predicción: `marketplace` es valor legal del enum `actor`, no puede producir fuera-de-enum. Mecanismo confirmado; mitad explicativa refutada. Evidencia sellada (artefacto en main: `state/output/p150_bulk_extract_vocab_comparison.md`, commits `c275d42`, `e3cbbe5`) mergeada a `main` vía PR #82 — `state/output/p150_bulk_extract_vocab_comparison.md`, citas ancladas a `main@a1a4084a` por diseño. No se cierra: lo que resta espera Etapa 3. Mecanismo reparado en main (PR #97, E5b): `infer_actor_level` consulta `source_type` antes de la regex; `help_center`/`policy_page`/`platform_doc` → `platform` (`bulk_extract.py:191-192`, función en línea 185). Residuo de pricing_page cerrado: PR #99 mergeado, rama borrada; el fix está en main. La mitad-predicción de la fila sigue esperando el cierre de la re-extracción (Etapa 3), sin cambio ·mov:S35 |
| P-151 | 24 propiedades con `enum` en 13 archivos de schema no están en la configuración de `vocab_check.py` en absoluto: el check nunca las visita porque no existen como key en `pipeline_vocabulary.yaml` (`vocab_check.py:244`, `for field_name, entry in vocab.items()`) — cero direcciones verificadas (ni `missing` ni `extra`), no una (`state/output/vocab_check_blind_spots.md`, Bloque 2, artefacto en main: `state/output/vocab_check_blind_spots.md`). Incluye `validation_status`, `failures`, `destination`, `issue_type`, `possible_actor_levels`, `possible_metric_types`, `entry_gate_decision`, `severity`, entre otras. Misma forma que el bug de platform de S26 (`Decision_Log_consolidado.md`, project files): 469 de 1,178 records que habrían fallado validación quedaron invisibles tres meses y medio porque nada los comparaba. Es la reparación de mayor retorno del grupo de enums: extender la cobertura de `vocab_check.py` una vez cubre las 24 propiedades a la vez, en vez de cerrar P-141, P-144, P-146 y P-149 una por una | decisión | ¿Cuáles de las 24 tienen hoy valores poblados en el corpus que caen fuera de su enum declarado? | DSC | artefacto en main — `state/output/vocab_check_blind_spots.md` (PR #77, `main@a1a4084a`). Mitad de vocabulario ejecutada en E2 (PR #91, D-247); mitad de schemas pendiente de la vía P-151 vs una-a-una (S6) ·mov:S34 |
| P-155 | El manifest de re-extracción de Sonnet declara `input_fingerprint` (`cf70dce389e7b0f17ac36a526a52ea6ae13a8448e9eea675187207faf7e1f906`) sin declarar método. No reproduce por ninguna variante probada. El input compartido quedó PROBADO por otra vía: tree-hash de `working/data_extraction/skeleton_batches/` idéntico (`60dbf926...`) en `base_sonnet`, `base_fable` y `main`. Además los dos manifests son asimétricos: Sonnet tiene 5 claves y nombres de counters distintos (`written_to_records` vs. `written`) | decisión | ¿Se normaliza el formato de manifest de re-extracción, o el script de Etapa 3 lee cada uno con su forma? | DSC | `etapa3_compare.py` leyó cada manifest con su forma — opción (b) ejercida de facto; sellar o revertir (S9) ·mov:S34 |
| P-156 | `CLAUDE.md` no tiene validador ni check de CI. Los tres jobs cubren `pipeline_vocabulary.yaml`, `signal_card_defect_check.py --fixtures` y el ledger. Las reglas que viven en `CLAUDE.md` dependen de que cada sesión las lea | decisión | ¿Se agrega check, o se acepta explícitamente como no verificable? | DSC | ·mov:S34 |
| P-157 | Ramas `legacy/s12-im-artifacts`, `preserve/s12-round1-75-cards` y `preserve/s12-round1-orphan-chain` existen en origin y ningún handoff reciente las registra | decisión | ¿Siguen teniendo propósito de preservación, o se archivan? | DSC | Instancia S35, cerrada: la rama `claude/pricing-page-source-type-k4ycey` existió sin registro; su PR (#99) fue mergeado y la rama borrada. Verificado: ausente de origin. La pregunta de la fila vuelve a cubrir 3 ramas ·mov:S35 |
| P-153 | CLASE: la restricción se escribe sobre la superficie anticipada, no sobre la que existe, y queda muda exactamente donde ocurre la falla. Tres instancias del mismo día. (i) Los prompts de Etapa 2 del benchmark especificaron directorios (`working_reextraction/<modelo>/`) y no ramas, commits ni pushes; la rama `claude/etapa-2-field-extraction-jyqwwj` es conducta emergente que ningún documento explica. (ii) `vocab_check.py` se configuró sobre los campos presentes en `pipeline_vocabulary.yaml` y no sobre las propiedades con enum presentes en los schemas: 24 propiedades en 13 archivos nunca se visitan (P-151). (iii) La regla de no-fetch de independencia se escribió como "durante la corrida" sin nombrar qué sesiones; se aplicó a la sesión de registro, desactivó el BLOQUE 0 y dejó el SHA de main sin verificación local. Seis instancias medidas en el repo, con archivo:línea: `field_population_audit.py:266-273` (hardcodea 7 campos — el script que mide el problema lo instancia); `CLAUDE.md:73-75` más su cadena de validación (la regla "sin adjetivos valorativos" se aplica como lista de 9 palabras); `field_lifecycle_trace.py:46-62` (8 consumidores hardcodeados); `ledger_path_check.py:25` (extensiones fijas, autoconstatada); `signal_card_defect_check.py:229-233` (7 campos de texto enumerados); esta misma fila cita `signal_card_defect_check.py` junto a los tres scripts anteriores, todos en `state/scripts/`, sin aclarar que `signal_card_defect_check.py` vive en la raíz del repo, no en `state/scripts/`. Tres instancias conversacionales de S31: regla sin destinatario, dos destinatarios en un mensaje, comando de registro enviado fuera de su carril | decisión | ¿Qué otras restricciones vigentes en el repo enumeran una superficie —lista de directorios, campos o rutas— en vez de definir la clase de la acción prohibida? | DSC | Instancia S32 de la clase: series Rule 1..N independientes entre archivos. Conteo a BASE (`83a70a25162489763bb05d1d40878b27dec8c63e`, medido con `grep -rln '^.*Rule 1' phases/*/contracts phases/*/modules .claude/skills`, serie por archivo verificada): 5 archivos — `phases/01-source-intake/contracts/source_intake_contract.md` (Rule 1-6), `phases/02-signal-extraction/contracts/signal_extraction_contract.md` (Rule 1-5), `.claude/skills/p1-convert-findings/SKILL.md` (Rule 1-5), `.claude/skills/p2-extract-signals/SKILL.md` (Rule 1-11, unificado por PR #85), `.claude/skills/p1-extract-records/SKILL.md` (Rule 1-8). Además: `destination` fuera de la cobertura de `vocab_check.py` (no aparece en el script ni en `pipeline_vocabulary.yaml`; declarado en `phases/01-source-intake/schemas/converter_manifest.schema.json:90` y `phases/02-signal-extraction/schemas/signal_converter_manifest.schema.json:169`). Instancia conversacional S34: E2 citó `state/scripts/vocab_check.py` y vive en la raíz (`./vocab_check.py`). Instancia S34 con costo medido en artefacto de estado: `FROZEN_HEADING_RE` enumeraba una superficie ("la frase en cualquier `state/*.md`") en vez de definir la clase (un artefacto declarado de congelación), y quedó muda exactamente donde falló — el snapshot que las sesiones leen para orientarse afirmó como superficie congelada un fragmento del ledger (P-180). Corrección S35 del conteo: son AL MENOS 7 archivos con serie propia, no 5. El grep original (`phases/*/contracts phases/*/modules`) no alcanza `phases/01-source-intake/data-extraction/` — un nivel más profundo. Se suman: `phases/01-source-intake/data-extraction/modules/extraction_converter.md` (Rule 1-6, líneas 201-219) y `phases/01-source-intake/data-extraction/contracts/data_extraction_contract.md` (Rule 1-6 headers, líneas 303-334). Causa raíz (R-D): el glob enumeró la superficie anticipada, no la existente — instancia de la clase que esta misma fila describe. El scope de la ejecución de D-257 (namespacing) es la lista de 7 ·mov:S35 |
| P-168 | Phase 3 corrió sobre el corpus de 75 cards, y su corrida completa existe en el historial de `main`, no solo en `preserve/*`: `2505539d` es ancestro de `origin/main` (verificado con historia completa), y el rango `2505539d..3dfd73ca` contiene la cadena Entry Gate → Splitter → Indexer → 7 scanners → Builder (23 TCs) → Validator. Los artefactos fueron borrados deliberadamente por `3dfd73ca` ("Clean up test run data"); por eso `input/signal_cards_round_*.md`, `working/index/card_index.jsonl` y los scans no existen en el árbol vigente. Lo que no existe es una corrida de Phase 3 sobre el corpus vigente de 29 cards | decisión | ¿Se recuperan los artefactos de la corrida borrada desde el historial, se replica la corrida sobre el corpus vigente, o se declara histórica sin valor operativo? | DSC | verificado — pasada S34-3 con `git fetch --unshallow`; corrige el enunciado previo, que se apoyaba en una medición sobre clon shallow ·mov:S34 |
| P-177 | `ACTOR_LEVEL_TO_DOMAIN` (`signal_to_markdown.py:79-88`) sin `platform` ni `third_party` — D-197/D-198 nunca cruzaron al puente; `domain` se deriva del campo equivocado (S26 §10); `"competition"` inalcanzable | hecho | ¿El mapa `ACTOR_LEVEL_TO_DOMAIN` incluye `platform`/`third_party` como claves, y puede producir `"competition"` como valor? | CC | mapa confirmado en `signal_to_markdown.py:79-88` (claves: buyer, seller, product, marketplace, mixed, source, unknown — sin `platform` ni `third_party`, ambos valores legales de `actor` en `pipeline_vocabulary.yaml`); `domain` enum de `card_record.schema.json:41` incluye `competition`, el mapa nunca lo produce (A8) ·mov:S34 |

**Cerrados en esta corrección (ya hechos, verificados contra el repo — salen de la tabla):**

- **P-075** — cerrado-ya-hecho — `working/data_gathering/diagnostics/qa_notes/`, 59 archivos
- **P-077** — cerrado-ya-hecho — 79 diagnósticos ya clasificados en `working/data_gathering/diagnostics/part_4/` (43 `__F-X05.json` + 36 `__F-X07.json`, cada uno con `verification_status`); 0 refs a `scope_exploration`
- **P-082** — cerrado-ya-hecho — `working/source_intake/source_intake_gpt_recovery/` solo tiene `.gitkeep`, 0 archivos reales
- **P-154** — cerrado — medido con `ledger_path_check.py`: 10 rutas citadas no existen en `main`, 8 son project files citados por diseño, 2 ya tienen fila propia (P-142/P-143 y P-109). I7 no se implementa; el script queda como herramienta manual en `state/scripts/`
- **P-102** — cerrado — implementada en `CLAUDE.md`, sección "Branch state verification" (PR #81, main); cubre los tres casos: `fetch --prune`, ramas que existen sin existir en origin, columnas Ahead/Behind
- **P-145** — cerrado — D-241 ejecutada, PR #85; renumeración Rule 1-11 verificable en `.claude/skills/p2-extract-signals/SKILL.md`
- **P-164** — cerrado — PRODUCTOR-ENCONTRADO: `phases/02-signal-extraction/scripts/signal_to_markdown.py:414` escribe `input/signal_cards_round_{n}.md`; ejecutado en el commit `2505539d` ("G1: generate signal_cards_round_1.md from 75 Signal Cards"), ancestro de `main` (`git merge-base --is-ancestor 2505539d origin/main`). El eslabón existe; el script se autodocumenta (docstring líneas 2-7), ningún módulo lo documenta (A8)
- **P-169** — cerrado — PRODUCTOR-ÚNICO-IDENTIFICADO: 118 commits de autor `Claude` tocan `working/data_extraction/records/` (38 con el prefijo `p1-extract-records:`, el resto con mensajes `records ...`/`extraction ...`/`Add ...`), acumulado 25→1,178 (`22823aa6` "batch_001 complete (25/1178 records)" → `41b83a82` "records 1171-1178 (final)..."), coincidente con `extraction_prepare_manifest.json` (`total_skeletons: 1178`, `batches_written: 48`) y `extraction_converter_manifest.json` (`records_written: 1178`, `skeleton_failures: 0`), ambos `status: complete`. `64c0a7d4` remapea (203 `claim_type` + 17 `uncertainties`), no crea records nuevos. Matiz que pasa a fila propia (P-196): la firma vive en los mensajes de commit, no en el artefacto. Corrección del cierre: el veredicto "productor único" era incompleto. Secuencia real verificada con historia completa: `22823aa6`/`d0109d78` producen los primeros 75 records vía la skill `p1-extract-records`; `2c44fa8f` produce 1,103 con `bulk_extract.py`; `3e5a8a27` los revierte ("Deleted 1103 heuristic-script records... quality audit revealed systematic degradation in subject_exact, evidence_role, local_qualifiers, and parser_notes"); `a6870291..41b83a82` los re-producen por lotes vía la skill, marcados 'LLM'. El productor del corpus vigente es la skill, no el script. Confirma D-179 por el ángulo inverso
- **P-143** — cerrado — duplicado verbatim de la fila hermana (P-142; mismo enunciado, misma pregunta, mismo estado); fusionada en esa fila en la pasada S35
- **P-167** — cerrado — pregunta respondida: 0 ocurrencias de campos métricos/temporales bajo `phases/03-inventory-mapping/` (verificado S34 y re-verificado S35). El residuo de diseño vive en las filas del puente (P-163, P-165, A8); esta fila no agrega decisión propia
- **P-175** — cerrado — medido y replicado: unknown 722/1,178 en records, 18/29 en cards. La medición completa vive en la fila del eje (P-159), que es donde la decisión se toma; fila absorbida por esa fila
- **P-190** — cerrado — su pregunta verificable (¿PR #90 mergeado?) quedó respondida: merge `879cc64`. Las señales de health check que la fila registraba son insumo del health check de ~25-ago y viven en los handoffs S33/S34; la fila no guarda nada más

---

## B. Ya verificados en S28 — no requieren Run 2, esperan decisión

| ID | Enunciado | Clase | Qué falta | Dónde | Estado |
|---|---|---|---|---|---|
| P-125 | Regla 5 de `p2-extract-signals/SKILL.md` contradice campo 13 de `signal_converter.md` | hecho | Cláusula de ruteo de anclas temporales a `time_scope_raw`, dependiente de P-126 y P-119 | CC | la contradicción entre regla 5 y campo 13 quedó resuelta en el commit `f884099`; pendiente la cláusula de anclas temporales a `time_scope_raw`. R6 habilita la cláusula pendiente de ruteo de anclas; redacción va con la preparación de la re-codificación ·mov:S34 |
| P-131 | Dos versiones de `Blueprint_Phase_2_Signal_Extraction.md` con la misma pretensión de fuente única | hecho | Retirar una. Vive en project files, no en el repo | DSC | verificado ·mov:S34 |
| P-133 | El análisis de diferencias de S27 se llamó "health check", término reservado al componente E del Blueprint | hecho | Corrección de término en el registro | DSC | verificado ·mov:S34 |
| P-126 | La magnitud del caso mezclado en `time_scope_raw` (verbatim temporal junto con material que no es scope, en el mismo valor) no está medida de forma reproducible: dos corridas del mismo clasificador sobre el mismo corpus dieron 469 y 489 records en la categoría MEZCLADO, y ninguna cifra es citable (`state/output/multiples_anclas_verbatim_check.md`, artefacto en main: `state/output/multiples_anclas_verbatim_check.md`; cifra previa de 469, `state/output/time_scope_raw_distribution.md:58`, artefacto en main: `state/output/time_scope_raw_distribution.md`). Formulación original de S28 (`Decision_Log_update_session28.md:174`, project files): ninguno de los seis archivos que el ejecutor lee dice qué hacer cuando el snippet contiene material temporal verbatim mezclado con material que no es scope; la única regla que existe es "null si está ausente". `phases/02-signal-extraction/modules/signal_converter.md:171` excluye metadata de acceso/record de `time_scope_raw` y la rutea a `normalization_notes` — esa regla cubre el caso de metadata de acceso, no el caso mezclado general; afirmar que sí lo cubre fue un error introducido en PR #71. Cuelga de la misma deuda de campos de Phase 1 que P-136, P-129b y el drift de `actor_level` señalado en `signal_converter.md:159`. (bloque A, decisión 2a) | decisión | Extraer verbatim y resto a `normalization_notes`, o conservar string completo y marcar contaminación | DSC | verificado ·mov:S34 |
| P-119 | Múltiples anclajes temporales verbatim en `time_scope_raw`: universo no confirmado — entre 9 y 16 de 1,178 records, según cómo se resuelvan cuatro ambigüedades del léxico usado para clasificar (`annual` vs. `anual`; `7+` como número+unidad; `half` solo vs. la frase completa `year and a half`/`año y medio`; intensificadores sobre deícticos ya listados, como `most recently` sobre `recently`). Dos corridas del mismo léxico sobre el mismo corpus dieron 16 y 9 (`state/output/multiples_anclas_verbatim_check.md`, artefacto en main: `state/output/multiples_anclas_verbatim_check.md`; cifra previa de 16, `state/output/time_scope_raw_distribution.md:57`, artefacto en main: `state/output/time_scope_raw_distribution.md`). (bloque A, 2b) | decisión | Concatenar con separador, un ancla y resto a notas, o splittear la card | DSC | verificado. R9(d) cubre multi-métrica, no multi-ancla temporal; universo se re-mide bajo R6 en la re-codificación ·mov:S34 |
| P-127 | Condición de override de `actor_level` delega en juicio no acotado (bloque A, 3) | decisión | Eliminar la segunda condición, o acotarla a criterio verificable. No hay criterio propuesto y los casos no existen | DSC | verificado ·mov:S34 |
| P-128 | Hueco en la tabla de `source_type` para reporte en tercera persona (bloque A, 4). Obligatoria en los 305 records con `actor_level: unknown` | decisión | `source` es el polo seguro; `seller` crea polos falsamente cross-actor | DSC | verificado ·mov:S34 |
| P-129b | Valores poblados en el corpus vigente caen fuera del enum del schema Y del enum de `pipeline_vocabulary.yaml` simultáneamente — no es divergencia schema-vocab, en estos tres campos ambos enums coinciden exactamente: `actor_level` 15/1178 (`third_party_observer`×10, `creator`×5 — ya señalado como drift de Phase 1 en `phases/02-signal-extraction/modules/signal_converter.md:159`), `product_type_if_explicit` 207/1178 (texto libre, ej. "online courses", "digital_download"), `metric_type` 186/1178. Contraste: `product_type_if_explicit`, `metric_type` y `uncertainties` están 0/29 fuera de enum en Signal Card (`state/output/field_population_signal_cards.md:17,18,26`: Poblado 29/29, 29/29 y 27/29 respectivamente, Fuera de enum 0/0/0 en los tres — el enunciado previo de esta fila leyó esas líneas como "0/29 poblado", lo cual no concuerda con la propia cita, que dice Poblado, no Vacío) contra 207/1178 y 186/1178 fuera de ambos enums, y 155/1178 fuera del enum del vocab (0/1178 fuera del schema), respectivamente, en Extraction Record (`state/output/field_population_extraction_records.md:24,25,33`). De lo poblado en Signal Card, una parte cruza como `"unknown"` — valor legal del enum sin información, no vacío ni fuera de enum: `product_type_if_explicit` 27/29, `metric_type` 18/29 (`state/output/phase1_to_card_field_fate.md:818-819,825`, artefacto en main: `state/output/phase1_to_card_field_fate.md`). Corrección de alcance (reporte de la conversación DSC, no verificado contra el repo en esta pasada): además de los 207 valores de `product_type_if_explicit` fuera de enum, hay 642 records con el campo en `null`, y el `oneOf` de `data_extraction_record.schema.json:173-215` no tiene rama `null` — esos 642 son schema-inválidos, sin detectarlo la conformidad por la misma razón que D-224 registró para el tipo nullable. El alcance real del campo es 849 de 1,178 en algún estado inválido (642 null + 207 fuera de enum), no 207 | decisión | Re-normalizar los valores fuera de enum en los tres campos (mapear a un valor válido, o agregar los valores recurrentes al enum si son legítimos), o aceptar la pérdida de estructura | DSC | verificado — `state/output/field_population_extraction_records.md:22,24,25` y `state/output/field_population_raw.json` (artefacto en main: `state/output/field_population_raw.json`, Run 3). Dirección fijada por D-249 (corpus → enums); ejecución = re-codificación acotada; el subconjunto metric_type espera el eje (P-159) ·mov:S34 |
| P-132 | El detector mide snippet → card, no campo del ER → campo de la card (`signal_card_defect_check.py:275-285`, patrones en `:139-150`). Sobre los 11 casos de pérdida trazados en `phase1_to_card_field_fate.md` (artefacto en main: `state/output/phase1_to_card_field_fate.md`), detecta 1 (SC-R1-010); su segundo flag, SC-R1-016, tiene origen null y no pertenece a esos 11 (`state/output/time_scope_raw_distribution.md`, Bloque 3, misma rama). Intersección de las dos mediciones: 1 de 11. Además, se mergeó sin satisfacer la precondición de calibración de D-212 Carga A — los tres casos originales no existen | decisión | Definir qué debe medir el detector — pérdida snippet→card (lo que mide hoy) o pérdida ER→card (lo que mide `phase1_to_card_field_fate.md`) — y recalibrar según esa definición, o aceptar el detector con su alcance actual declarado | DSC | verificado ·mov:S34 |
| P-134 | Segunda corrección: no es problema de enum. `evidence_role` aparece en CLEAN FIELDS de `vocab_check.py` (0 divergencia) y la sección OPEN-STRING FIELDS del mismo check reporta `(none)` — no existe en el repo ningún campo donde el vocab declare enum cerrado y el schema lo declare string libre. El hueco es de puente, no de enum: el renombrado a `evidence_base` ocurre en el paso de renderizado a markdown (`signal_to_markdown.py:274`, línea literal `f"Evidence base: {evidence_role}"`), y `card_record.schema.json:47` declara su propio campo `evidence_base` (string libre, sin enum) — con un nombre distinto a `evidence_role`, por lo que `vocab_check.py` nunca lo compara contra el enum de origen (el check empareja por nombre exacto). El valor cruza vía el texto del label; la restricción de enum no cruza porque el puente no lo intenta, no porque el checker lo deje pasar | decisión | Extender el puente para que el enum sobreviva al renderizado (precedente D-126), o aceptar que `evidence_base` quede como texto libre | DSC | verificado — corrida local de `vocab_check.py` (CLEAN FIELDS incluye `evidence_role`; OPEN-STRING FIELDS: `(none)`), confirmado también en runs de CI `30504740061`/`30504959916`; `card_record.schema.json:47`; `signal_to_markdown.py:274`; `state/output/field_lifecycle_card_record_bridge.md:18` (artefacto en main: `state/output/field_lifecycle_card_record_bridge.md`, Run 3) ·mov:S34 |
| P-139 | Tres campos declarados en `pipeline_vocabulary.yaml` sin ningún campo de schema que los declare: `verification_status`, `allowed_verbs`, `forbidden_language` — reportados por `vocab_check.py` bajo `VOCAB FIELDS WITH NO MATCHING SCHEMA FIELD FOUND` (sección informativa: no gatea el exit code, a diferencia de DIVERGENCIAS y OPEN-STRING FIELDS). `verification_status` es campo obligatorio y central de Phase 0 (`phases/00-data-gathering/reference/data_gathering_project_instructions_v4_5.md`: "Every finding must include... verification_status"; "verification_status must be exactly one of: direct_verified, blocked_url_index_verified, could_not_verify"), pero Phase 0 no tiene ningún `*.schema.json` en el repo. Misma clase que P-129 (divergencia schema↔vocab), dirección opuesta: ahí el check encontró coincidencia de nombre y comparó valores; aquí no hay schema con el que comparar, así que el gap no cae en DIVERGENCIAS ni en OPEN-STRING FIELDS — el check no lo reporta como divergencia porque solo compara donde hay match | decisión | Escribir el schema que falta (Phase 0), o aceptar que estos tres campos vivan solo en el vocab sin validación estructural | DSC | verificado — corrida local de `vocab_check.py` y runs de CI `30504740061`/`30504959916` (sección `VOCAB FIELDS WITH NO MATCHING SCHEMA FIELD FOUND`); `pipeline_vocabulary.yaml:172-182,346-362` ·mov:S34 |
| P-136 | Dos casos distintos, medidos por separado en S35. (a) RESUELTO: `author_or_actor_if_available`, null 1,178/1,178 y 0 consumidores más allá de la copia en bloque de `signal_prepare.py:81` — ausente de `signal_card.schema.json`, del puente (`signal_to_markdown.py`) y de Phase 3; quitar el requisito no rompe a nadie. (b) ABIERTO, y es lo que la fila conserva: `snippet_context_before` y `snippet_context_after`, también null 1,178/1,178, PERO con lector activo — `signal_card_defect_check.py:219-221` los concatena con `snippet_primary`, y son consumo normativo en `p2-extract-signals/SKILL.md:56` y `signal_converter.md:134`. Quitar el requisito aquí desactiva un detector | decisión | Para (b): ¿se hace exigible el campo, se re-corre Phase 1, o se acepta que el detector opere sobre snippet truncado? (a) solo espera ejecución | DSC | verificado ·mov:S34 |
| P-078 | Documentación de Signal Extraction fragmentada. Dos protocolos obsoletos contra el código en project files | decisión | Reencuadrado por el diseño de S29: blueprints viven en project files. Lo que queda es jubilar los dos obsoletos | DSC | verificado ·mov:S34 |
| P-116 | Sin mecanismo para jubilar documentos obsoletos. Debilitado como causa del 28%, reforzado por P-129 y P-131 | decisión | Mecanismo, o aceptar el costo caso por caso | DSC | verificado ·mov:S34 |
| P-121 | El archivado del manifest es prosa, no mecanismo | decisión | Construir el mecanismo, o aceptar la mitigación | CC | verificado ·mov:S34 |
| P-097 | Phase 1b corrió sin validador (D-140). Diagnóstico corregido en S28: la ausencia de `validation_status` es conformidad con el schema | hecho | No existir no es una decisión tomada, es ausencia sin resolver | CC | promover-a-decision — sin script en `main`/`legacy/*`/`preserve/*`/`git log --all`/objetos unreachable (`git fsck`); solo contrato+schema+`.gitkeep` vacío. No existir no es una decisión tomada, es ausencia sin resolver ·mov:S34 |
| U-4 | `working/data_gathering/part4_failure_mode_breakdown.json` sin dueño | hecho | Qué hacer con él (borrar o conservar por valor diagnóstico) es juicio del operador | CC | promover-a-decision — huérfano confirmado (0 refs en `main`, 0 refs en 105 objetos `unreachable` de `git fsck --full --unreachable`); qué hacer con él (borrar o conservar por valor diagnóstico) es juicio del operador ·mov:S34 |
| P-159 | Eje de `metric_type` sin definir: 22 bloqueos, 15 con magnitud real, 13 bien capturadas sin destino; el enum mezcla magnitudes con no-magnitudes | decisión | Definir el eje que separa magnitudes de no-magnitudes en el enum | DSC — insumo E1: 35 packets marcaron `metric_type_unclear` en intake (A6) | Medición completa disponible (E10, BASE `bd53e18`): enum sin divergencia (21 valores); records 1,178 sin nulls — `unknown` 722, en-enum 991, fuera de enum 186 en 9 valores (`rate` 67, `volume` 54, `count` 34, `buyer_count` 8, `subscriber_count` 7, `share` 6, `commission_rate` 5, `traffic` 3, `discount_rate` 2); 1 solo array en corpus (`["revenue","payout"]`), no 15 — C41/C42 son veredictos de la muestra sin escribir de vuelta; Signal Cards `unknown` 18/29. La fila espera diseño, no medición ·mov:S34 |
| P-160 | Salida de R7 en Phase 3 sin elegir (D-251): mapear `unknown` a valor existente vs entrar sin actor; 1/60 hoy, escala con la re-codificación | decisión | Elegir entre mapear a valor existente o entrar sin actor | OP+DSC (A5) | ·mov:S34 |
| P-161 | No existe campo de agregación ni de cambio: nivel, cambio y share conviven en `metric_value_raw` (C31, C41, C42) | decisión | Definir campo(s) de agregación/cambio — alimenta P-159 | DSC | ·mov:S34 |
| P-162 | Estrechamiento descendente de enums, patrón medido: los tres `phase_1_only` con uso real en packets (2/35/355, E1) mueren en el enum de ER; uncertainties 16→14→13 hacia Signal Card; actor `unknown` ausente de `card_record`. Toda medición de uso declara capa | decisión | Decidir si el estrechamiento es intencional o defecto de puente | DSC | ·mov:S34 |
| P-163 | `platforms` no llega a Phase 3 como campo; `entities` se reconstruye best-effort (`03_indexer.md:20-23`) | decisión | Agregar `platforms` como campo de primera clase, o aceptar la reconstrucción best-effort | A8 | verificado — `03_indexer.md:20-23` confirma extracción best-effort de entities; `platforms` ausente de la lista de campos de la línea 20 ·mov:S34 |
| P-165 | Los 7 scans sin código; Phase 3 no reproducible en su fase declarada mecánica | decisión | Implementar los scanners, o aceptar el paso como manual | DSC (A8) | verificado — 0 archivos `.py` bajo `phases/03-inventory-mapping/`; solo `04_scanner.md` (módulo) y `scan_artifact.schema.json` (schema) ·mov:S34 |
| P-170 | `claim_type`/`evidence_role` sin regla general: D1/D2 cubren dos fronteras; 5 casos residual-lite por causa estructural. La regla de desempate del contrato (Rule 4, `extraction_converter.md:213`) es inescribible para estos dos campos — `claim_type_unclear` y `evidence_role_unclear` no existen en vocab ni schemas, misma forma que el bloqueo de R9(a); la única glosa normativa del par (Rule 2 → `local_context`, `extraction_converter.md:207`) corresponde al único valor con 0 usos en 1,178 | decisión | Escribir la regla general, o aceptar el residuo caso por caso | DSC | verificado — `extraction_converter.md:207` (Rule 2) y `:213` (Rule 4) confirmados; 0 ocurrencias de `claim_type_unclear`/`evidence_role_unclear` en `pipeline_vocabulary.yaml` o schemas ·mov:S34 |
| P-171 | Dónde vive un comparador (C6, C16): `metric_value_raw` vs `parser_notes`, sin regla | decisión | Definir dónde vive el comparador | DSC | ·mov:S34 |
| P-172 | Formato de preservación de bloques (C42): R9(d) decide tipo, no forma | decisión | Definir la forma de preservación | DSC | ·mov:S34 |
| P-173 | Marca de atomicidad inexistente: "record que debió partirse" no tiene campo ni valor en vocab ni en los tres schemas. De las tres salidas de C17 (partir / rechazar / aceptar-con-advertencia) solo la tercera exige la marca | decisión | Agregar el campo/valor de atomicidad, o aceptar la ausencia | DSC | ·mov:S34 |
| P-174 | `none` vs `[]` en `uncertainties`: 345/1,172 resignificados por D-245 ("none" pasa a afirmación fuerte); normalización del corpus sin decidir | decisión | Normalizar `none` vs `[]`, o aceptar la mezcla | A7 | ·mov:S34 |
| P-178 | La lectura obligatoria del ejecutor P2 (`SKILL.md:9-19`) no incluye `pipeline_vocabulary.yaml` (0 menciones) — mitad huérfana de P-129 | decisión | Agregar `pipeline_vocabulary.yaml` a la lectura obligatoria, o aceptar la omisión | DSC | verificado — `.claude/skills/p2-extract-signals/SKILL.md:9-19` no lista `pipeline_vocabulary.yaml`; 0 menciones en el archivo completo ·mov:S34 |
| P-179 | `source_packet_conversion_template.md` en dos capas divergentes: repo 316 líneas (fix Rule 6/`indirect_verified`, `d640852`) vs project files 301 sin la cláusula; marcador fósil "Gap 1" en ambas (línea 23) | decisión | Reconciliar las dos capas | OP (capa dueña) + ejecución | verificado (lado repo) — `phases/01-source-intake/reference/source_packet_conversion_template.md` tiene 316 líneas; línea 23 contiene "Gap 1"; commit `d640852` existe en el historial ·mov:S34 |
| P-181 | P-122 vs árbol: cerrado en S28 como "retirado" y `signal_validation.schema.json` existe en `main` (:35) | decisión | Resolver la clase: cierre cuyo enunciado contradice el árbol (via sello S4 / I.13) | CC | verificado — `signal_validation.schema.json` existe en `main` (confirma la contradicción). Sello S4 aplicado en E-S4-S36 (PR #110): veredicto 1c = DEPENDIENTES — los tres contratos de validador enuncian solo los enums de estado, no la estructura del documento de salida (`source_intake_validator.md:35-50`, `data_extraction_validator.md:33-47`, `signal_extraction_validator.md:34-48`); rama MARCAR ejecutada sobre los 4 schemas (`$comment` declarando el paso sin ejecutor que persista, primera propiedad de cada raíz), ninguno retirado. Ya no DIFERIDA-SELLO. I.13 no re-examinado en esta pasada (fuera de scope de E-S4-S36) ·mov:S34 |
| P-182 | Estados a nivel card/record/packet declarados en 4 schemas y documentados en el validator, sin productor conocido — pero NO son los mismos 4 valores en los 4: `signal_validation.schema.json`, `signal_inventory_gate.schema.json` y `data_extraction_validator.schema.json` declaran 4 (`pass`/`pass_with_flags`/`rework`/`reject`); `source_intake_validation.schema.json` declara 5, agrega `parking_lot` — coincide con `source_intake_validator.md:39-44`, que sí lista `parking_lot` como quinto estado permitido. El enunciado previo ("cuatro schemas declaran los cuatro valores") no se sostenía; la corrección ya estaba escrita en `state/output/vocab_check_blind_spots.md:225-229` sin propagarse a esta fila | decisión | ¿Cuántos schemas declaran validation_status, cuántos valores cada uno, y qué script los escribe? | liga P-097/P-151 | verificado — 4 schemas declaran `validation_status`/`status` a nivel card/record/packet (`signal_validation.schema.json`, `signal_inventory_gate.schema.json`, `source_intake_validation.schema.json`, `data_extraction_validator.schema.json`), pero solo 3 comparten el mismo conjunto de 4 valores; `source_intake_validation.schema.json:35-39` declara 5 (agrega `parking_lot`, línea 38). 0 scripts los escriben (sin cambio). Corrección de enunciado por E-S4-S36 (PR #110); no cierra la fila ·mov:S34 |
| P-187 | Contradicción de registro sobre `verification_status`: la tabla de desincronizados (S23/S26) lo da por `deprecated` en los protocolos de DG; P-139 lo cita como enum vigente de Phase 0 desde el mismo protocolo | decisión | Resolver cuál registro tiene autoridad | DSC | ·mov:S34 |
| P-189 | Overhead como tiempo: comprometido desde S27, nunca medido; siete sesiones comparables | decisión | Medir, o aceptar sin medir | health check | ·mov:S34 |
| P-191 | Regla candidata: retirar un valor de vocabulario exige conteo en todas las capas (packets, records, cards, skeletons) — validada por E1: los tres valores reportados "0 usos" miden 2/35/355 en packets; tres retiros erróneos atrapados. Replicado por corrida independiente a segundo BASE, doble método (python+jq), resultado idéntico | decisión | Adoptar la regla como check formal, o dejarla como práctica | DSC | ·mov:S34 |
| P-192 | Solapamiento estructural entre enums: `comparative_commentary` y `derived_calculation` existen en `claim_type` Y en `evidence_role` (vocab + schema de ER). Un codificador puede asignar el valor al otro campo sin salirse de enum — toca C25, C55, C60 y alimenta el rediseño de P-170 | decisión | Resolver el solapamiento, o aceptarlo como ambigüedad conocida | DSC | verificado — `comparative_commentary` y `derived_calculation` confirmados en `evidence_role` (`pipeline_vocabulary.yaml:78-79`) y en `claim_type` (líneas 201-202). Reducida por medición S35 (E-S35-5): de los dos valores solapados, `derived_calculation` tiene 0 usos en toda capa donde el campo existe — su solapamiento es estructural pero no instanciado. El caso real es solo `comparative_commentary`: 18 en `claim_type` y 5 en `evidence_role` en records, 1 en `evidence_role` en cards. La decisión se acota a ese valor ·mov:S34 |
| P-193 | Marcas de fase de `uncertainties` sin validador ni semántica operativa: `vocab_check` no las lee (solo `field_population_audit`, script de medición); tres de los cinco valores marcados tienen uso real solo en capa packet (E1) y el ER las corta por schema | decisión | Decidir si las marcas se validan, se documentan o se retiran (hallazgo asociado de D-247) | DSC | verificado — 0 ocurrencias de `phase_1_only`/`phase_2_only` en `vocab_check.py`; `field_population_audit.py:273,322` sí las usa ·mov:S34 |
| P-199 | Existe una corrida completa de Phase 3 en el historial de `main`, borrada por `3dfd73ca` ("Clean up test run data"): rango `2505539d..3dfd73ca`, cadena Entry Gate → Splitter → Indexer → 7 scanners → Builder (23 TCs) → Validator. Los 113 TCs del Decision Log tienen ascendencia alcanzable. Reencuadra el diagnóstico de S33 ("los 7 scans no tienen código; Phase 3 no es reproducible"): correcto sobre el árbol vigente, ciego sobre el historial — existieron artefactos de ejecución, no necesariamente código | decisión | ¿Qué se recupera del rango borrado: artefactos de scan, card_index, TCs, o nada? Alimenta el diseño del puente (Turno 6) | DSC | verificado — pasada S34-3, historia completa tras `--unshallow` ·mov:S34 |
| P-200 | Los 35 packets con `metric_type_unclear` produjeron **54 records derivados, 50 con `metric_type: unknown`** (excepciones: 1 `price`, 3 `fee_rate`), por cruce directo de `source_packet_id`. Sustituye la cifra 44/40 del encargo, que no se sostuvo. La señal de duda del intake no se resuelve aguas abajo: es campo sin destino, no error de codificación | decisión | Alimenta el rediseño del eje (P-159); ¿se resuelve ahí o requiere regla propia de intake? | DSC | verificado — pasada S34-3, cruce directo ·mov:S34 |
| P-195 | Origen del corpus de 1,560 Signal Cards en 10 rounds no verificable: entró completo por `95e259e2` ("Extract project from tarball"), sin script en el diff. Es la misma cifra que declaraba `.claude/agents/inventory-mapping.md`, archivado por D-237 | decisión | ¿Se investiga el origen, o se parquea con condición de desparqueo? | DSC | verificado — `95e259e2` ancestro de `main`, mensaje confirma "10 rounds, 1,560 signal cards", 0 script en el diff ·mov:S34 |
| P-196 | La trazabilidad de producción de los records vive fuera del artefacto: el schema de extraction record no tiene campo de procedencia de herramienta; la evidencia de qué produjo cada record son los mensajes de commit (`p1-extract-records:`). Funciona mientras el historial esté completo y uniforme; falla ante clon shallow, regeneración o cambio de rama | decisión | ¿Se agrega campo de procedencia al schema, o se acepta explícitamente que la trazabilidad es del historial? | DSC | verificado — `data_extraction_record.schema.json` sin campo de procedencia/herramienta; 5 records de commits distintos sin firma de herramienta en `parser_notes` ·mov:S34 |
| P-197 | Los clones de sesión pueden llegar shallow y el recorte se lee como dato: sin `git fetch --unshallow`, `git log --diff-filter=A` sobre `working/data_extraction/records/` devolvió 1 commit contra 118 reales (reproducido en esta misma pasada, clon `--depth 50` de control). Misma familia que el "0 usos" medido en una sola capa (P-191): la herramienta recorta la superficie y el recorte se reporta como hallazgo | decisión | ¿Se agrega a la plantilla de encargos la verificación `git rev-parse --is-shallow-repository` antes de toda afirmación histórica? | DSC | verificado — esta pasada partió de un clon shallow; `git merge-base --is-ancestor 2505539d origin/main` dio "no" en shallow y "sí" tras `--unshallow`, invalidando P-194 y confirmando la clase de fallo que describe esta fila. Instancia de mayor costo medida: la afirmación "el corpus de Phase 3 vive solo en `preserve/*`" entró al ledger (reformulado de P-168) desde una medición shallow del redactor, y una fila nueva (P-194) se construyó sobre ella. Ambas se detectaron en la pasada siguiente al re-medir con historia completa. El redactor de encargos está tan expuesto a esta clase como el ejecutor ·mov:S34 |
| P-201 | `bulk_extract.py` está fuera de la ruta de producción del corpus vigente: su output (1,103 records, `2c44fa8f`) fue revertido por degradación de calidad (`3e5a8a27`) y re-producido por la skill `p1-extract-records`. El CONTRACT de Phase 1b manda implementar la rama de fallo en ese script (sección 11, ítem 3) y P-150 pide reparar `infer_actor_level` ahí — ambas apuntan a un componente sin uso demostrado | decisión | ¿La rama de fallo va en el script o en la skill? Afecta D-242/D-243, A9 y A12 | DSC | verificado — corrida independiente con historia completa (R-G aplicado) ·mov:S34 |
| P-202 | Phase 2 escribe Signal Cards sin filtrar por `verification_status` de la fuente: 18/29 cards vigentes (62.1%) trazan, vía extraction record → skeleton (`_source_finding_ids`), a un finding con `verification_status: blocked_url_index_verified`, contra 503/1169 (43.03%, excluidos los 9 corrompidos de P-204) en el corpus completo de findings. Cero ocurrencias de `verification_status` en `phases/02-signal-extraction/` o en `.claude/skills/p2-extract-signals/` — ningún módulo ni skill de Phase 2 lo lee ni lo filtra | decisión | ¿Dónde debe vivir el filtro (si se decide que debe existir) — módulo del signal converter, skill, o gate G1/entry-gate? | DSC | verificado — trazado programático de las 29 cards vigentes (`working/signal_extraction/cards/*.json` → `source_record_ids` → `working/data_extraction/records/*.json` → `source_packet_id` → `working/source_intake/skeleton_batches/*/*.json` → `_source_finding_ids` + `retrieved_from` como clave compuesta con `shard_id` del finding → `working/data_gathering/findings/*.json`), 29/29 resueltas sin huecos, 0 colisiones de clave compuesta sobre 1,178 findings; 18 traen al menos un finding `blocked_url_index_verified`. `grep -rn verification_status phases/02-signal-extraction/ .claude/skills/p2-extract-signals/` sin resultados ·mov:S36 |
| P-203 | El Source Packet final descarta el puntero al finding de origen: el skeleton (stage 1, `working/source_intake/skeleton_batches/*/*.json`) declara `_source_finding_ids` (array), pero `source_packet.schema.json` (`additionalProperties: false`) no lo declara y el paquete final (`working/source_intake/packets/*.json`, 688 archivos) no lo retiene — 0/688 con el campo. La trazabilidad card→finding depende de un artefacto intermedio mutable (`working/source_intake/skeleton_batches/`), no del artefacto canónico | decisión | ¿Se propaga el puntero a `source_packet.schema.json`, o se declara explícitamente que la trazabilidad a nivel finding es propiedad efímera de los skeletons, no del artefacto canónico? | DSC | verificado — `grep -n source_finding_id phases/01-source-intake/schemas/source_packet.schema.json` sin resultados; muestra de skeleton (`working/source_intake/skeleton_batches/batch_004/skeleton_SP-compass_artifact_wf-28f59dc7-8351-48a1-bcaa-ff9992b8fe70_text_markdown-015.json`) confirma `_source_finding_ids` presente; `grep -l finding_id working/source_intake/packets/*.json` → 0 de 688 ·mov:S36 |
| P-204 | 9 de 1,178 findings tienen `verification_status` corrompido: el valor arrastra contenido de la siguiente sección del shard tras `\n\n---\n\n## `, p. ej. `"direct_verified\n\n---\n\n## Affiliate commissions are the only public percentage disclosures..."`. Los 9 provienen del mismo shard (`compass_artifact_wf-4ef0d94a-344f-48de-a6a0-29bd09258ed5_text_markdown_normalized`): 5 `F-*` (F-07, F-12, F-26, F-30, F-31) y 4 `F-P*` (F-P05, F-P11, F-P15, F-P16) | decisión | ¿Se corrige el dato de los 9 findings, o el parser que los produjo (`phases/00-data-gathering/scripts/parse_dg_shard.py`)? | DSC | verificado — barrido de los 1,178 archivos de `working/data_gathering/findings/*.json`: 1,169 con `verification_status` en {`direct_verified`, `blocked_url_index_verified`, `could_not_verify`} (666/503/0), 9 con valor fuera de esas tres cadenas, las 9 con el patrón de arrastre citado, todas del mismo shard ·mov:S36 |
| P-205 | `docs/pipeline_flow.md` referencia rutas `upstream/...` en 73 líneas; ese directorio no existe desde la reestructuración a `phases/` (commit `242318bf`, 2026-04-11). El propio documento se autodeclara obsoleto en su cabecera (líneas 3-4: "OBSOLETO — describe la estructura pre-restructure 242318b. CLAUDE.md es el mapa vivo.") pero sigue vigente en el árbol y se cita como procedencia en al menos 3 PRs de esta sesión (#108, #109, #110) | decisión | ¿Se actualiza el documento a rutas `phases/...`, o se archiva/retira dado que ya se autodeclara obsoleto? | DSC | verificado — `ls upstream` → No such file or directory; `grep -c 'upstream/' docs/pipeline_flow.md` → 73; cabecera líneas 3-4 confirmada ·mov:S36 |
| P-206 | El veredicto de los 11 checks de `signal_extraction_validator.md` se computa dentro del loop de `p2-extract-signals` (`SKILL.md:42`, `signal_converter.md:191,205-211`) y no se persiste en ningún campo ni archivo: se consume una sola vez para decidir `cards/` vs `signal_gpt_recovery/` y para poblar `issues_for_this_skeleton` con 8 códigos de tipo de problema (`signal_converter_manifest.schema.json:128-145`) que no corresponden 1:1 a los 11 checks nombrados. `signal_card.schema.json` no declara `checks` ni `validation_status` (`additionalProperties: false`). Mismo tema que P-181/P-182, ángulo del productor en vez del schema | decisión | ¿Dónde se persiste el veredicto estructurado (los 11 valores `pass`/`flag`/`fail`/`not_applicable` con su nombre de check), y en qué schema entra — `signal_validation.schema.json` marcado en esta misma pasada (S4), u otro? | liga P-181/P-182 | verificado — `signal_converter.md:205-211` (consumo de routing citado); `grep -n "checks\|validation_status" phases/02-signal-extraction/schemas/signal_card.schema.json` sin resultados; `signal_converter_manifest.schema.json:128-145` (8 códigos, ninguno de los 11 nombres de check) ·mov:S36 |

**Cerrados en esta corrección (decisión ya ejecutada, verificada — salen de la tabla):**

- **P-135** — CI existe: `.github/workflows/ci.yml`, dos jobs (`vocab-check`, `signal-card-defect-check --fixtures`), sin `continue-on-error`. Mergeado en PR #66 (`claude/ci-vocab-and-defect-checks`, merge `214dfe6`). Probado en las dos direcciones sobre el mismo PR: run `30504740061` (commit `d3f5954`) falló con exit 1 — `vocab_check.py` reportó divergencia real de `uncertainties` (`extra in schema: anecdotal_single_source, author_conflict_of_interest_possible, methodology_unclear`); run `30504959916` (commit `17ed6b3`), tras mergear el fix de PR #65 a la rama, pasó con exit 0 en los dos jobs.
- **P-129a** — Eran dos campos, no uno. `claim_type`: `statistical_data` agregado al vocab (PR #63, commit `7124833`; 90/1178 registros lo usan). `uncertainties`: `methodology_unclear` (107 apariciones), `anecdotal_single_source` (28), `author_conflict_of_interest_possible` (23) agregados al core del vocab (PR #65, commit `4b016db`) — 158 apariciones sumadas, 150/1178 registros con al menos uno de los tres (unión con solapamiento; no son 158 registros distintos). `vocab_check.py` en exit 0 en `main` hoy — 0 divergencias, `uncertainties` en CLEAN FIELDS.
- **P-137** — `extraction_converter.md` rutea fallos a `extraction_gpt_recovery/`, que no existe. El árbol y `CLAUDE.md` dicen `rejected_archive/` — cerrado — ruta corregida en `extraction_converter.md` a `rejected_archive/`, commit `e0928ed`.
- **U-2** — cerrado — verificado: `metric_type`, `metric_value_raw` y `metric_unit` están en `required` del schema; la premisa de la fila era falsa.
- **P-140** — cerrado — `signal_card_defect_check.py`: exit condicional en modo por defecto (PR #83, commit `5063182`); modo por defecto sale 1 hoy por 2 defectos `time_scope_loss` (dependientes de la decisión abierta de `time_scope_raw`); CI sin cambios, sigue en `--fixtures`; conteo real de fixtures es 21, no 22.
- **P-180** — cerrado — enunciado corregido antes de cerrar: no eran secciones sin escritor, sino un lector que encontró un escritor accidental. `FROZEN_HEADING_RE` buscaba la frase "superficie congelada" en cualquier `state/*.md` vía `rglob`; cuando el ledger ganó la fila P-180 —cuyo enunciado contiene la frase— el regex la matcheó y volcó filas del ledger (P-181, P-182, P-187, P-189, P-191…) dentro de la sección "Superficie congelada" de `state/STATE.md` (snapshot sobre `bd53e183`). La fila que documentaba el problema lo activó. La sección "Últimas 5 decisiones" tenía el vicio inverso: buscaba `decision.?log` en el repo, imposible por D-233. Resuelto por sello S5 (quitar ambas) y ejecutado en PR #95: removidas las dos secciones con sus constantes y funciones (`FROZEN_HEADING_RE`, `DECISION_LOG_NAME_RE`, `DECISION_ID_RE`, `find_decision_log_files`, `section_decisions`, `section_frozen_surface`); resto del snapshot idéntico, verificado antes/después contra salida temporal. Alternativa descartada y registrada: acotar el lector a un `state/superficie_congelada.md` nombrado — mecanismo con un solo uso real (benchmark S31-S32, cerrado) y archivo que casi siempre diría "ninguna vigente".
- **P-176** — cerrado — magnitud cero medida en todas las capas (P-191): `skeleton_invalid` está declarado en 10 archivos (3 módulos, 3 skills, 3 schemas de manifest y `phase1b-recovery/CONTRACT.md` — uno más que los 9 del enunciado original) y tiene 0 apariciones como valor en records, packets, cards, skeleton_batches, manifests y `*.py`. Se conserva la declaración; no se implementa productor. Condición de reapertura: que algún script llegue a escribirlo.
- **P-138** — cerrado — se acepta que `uncertainties` no cruza el puente. Medido: 0 consumidores en Phase 3 (6 módulos, 6 schemas, 12 skills) y 0 en el puente, que emite 7 etiquetas fijas (`signal_to_markdown.py:262,264,266,268,270,272,274`). No hay consumidor al que extender el puente. Si un rediseño de Phase 3 lo necesita, entra como campo nuevo, no como reparación de esta fila.
- **P-166** — cerrado — premisa vencida, con el diagnóstico corregido. La fila supone un "Comparability Check" del que faltaría el tramo de plataforma. Medido: 0 ocurrencias de "Comparability"/"Comparabilidad" en `phases/`, `.claude/skills/` y `docs/` — el objeto no existe bajo ese nombre en Phase 3. Las 4 ocurrencias de la cadena "comparab" (`phases/00-data-gathering/reference/research_directions_protocol.md:3,6,278` — "structurally comparable" / "incomparable outputs"; `phases/02-signal-extraction/contracts/signal_extraction_contract.md:12` — "unidades comparables") son prosa incidental sobre comparabilidad de findings y unidades, no definiciones de check. Los 8 self-checks del canon (`phases/03-inventory-mapping/reference/protocol_canonical.md:98-107`, sección "Self-check before delivering any candidate") y las listas de `06_validator.md` no tienen tramo de plataforma; la única mención de plataforma en el canon (`protocol_canonical.md:18`) es una nota de distribución de cards. No hay contraparte que escribir. Si el puente de Phase 3 necesita separación por plataforma, es diseño nuevo (cuelga de P-163), no de esta fila.
  Nota de método: el cierre se intentó en E-S35-6 con la cifra "0 ocurrencias de comparab", heredada del reporte E-S35-5 sin re-medir y con distinta sensibilidad a mayúsculas; el ejecutor lo detuvo por R-C. El fondo se sostuvo, el enunciado no.

---

## C. Decisiones de DSC — no tocan el repo

| ID | Enunciado | Clase | Dónde | Estado |
|---|---|---|---|---|
| P-117 | Asignación por fase duplicada entre `Blueprint_DSC.md` y `DSC_Consolidado.md`. Cerrado para Phase 2 por D-213 (puntero); abierto para las otras ocho | decisión | DSC | ·mov:S34 |
| P-118 | Sexta pregunta del DFPI sobre rendimiento observado de la asignación. D-206 la declara revisable sin decir contra qué | decisión | DSC | ·mov:S34 |
| P-061 | La pregunta del proyecto no está escrita limpia. Bloquea el DFPI *del proyecto* | decisión | OP | ·mov:S34 |
| P-062 | Criterio de parada del reconocimiento. Vinculado a P-061 | decisión | OP | ·mov:S34 |
| P-183 | Handoff_session32 y el transfer del carril del benchmark ausentes de project files — eslabón de D-233 | decisión | OP | ·mov:S34 |
| P-184 | Residual de P-113: `Decision_Router_v0_3_updated.md` sin Phase 1 en su modelo de zonas | decisión | OP/DSC | ·mov:S34 |
| P-185 | `Blueprint_Selector_v2.md` usa "Phasing" (nombre muerto) en ≥8 pasajes incluido su gate de salida | decisión | OP/DSC | ·mov:S34 |
| P-186 | `DSC_Operativo.md` coexiste con `DSC_Consolidado.md` sin marca de autoridad ni fecha — clase P-131 | decisión | OP | ·mov:S34 |

---

## D. Candidatos a parqueo — necesitan condición de desparqueo, no discusión

| ID | Enunciado | Condición de desparqueo propuesta | Estado |
|---|---|---|---|

**Parqueados:**

- **P-002** — Fase post-Expression Research sin definir — condición de desparqueo: cuando Phase 7 produzca expresiones
- **P-004** — Blueprint de Expression Research reconstruido sin uso real — condición de desparqueo: cuando Phase 8 corra por primera vez
- **P-059** — Direction creator-driven, estructuralmente distinta — condición de desparqueo: cuando se decida ampliar el subject
- **P-095** — Convención de nombrado de shards — condición de desparqueo: cuando Phase 0 vuelva a correr en volumen
- **P-096** — Iterar `shard_template_v3`. Sin movimiento desde S15 — condición de desparqueo: idem P-095
- **P-158** — Naturaleza de `claude/etapa-2-reextraccion-campos-cnb8bh` sin determinar (¿corrida Fable previa o push intermedio?) — condición de desparqueo: discrepancias de la re-codificación contra los veredictos, o necesidad de auditar el brazo Fable; revisión: al cerrar la re-codificación acotada. Dos comandos la cierran (`git show <rama>:criteria.md` en ambas + tree-hash de `working_reextraction/fable/`)
- **P-188** — Carril de recovery congelado con 4 decisiones de S25 sin representación: trazabilidad finding→packet rota (recovery_id); route_unrecoverable declara validar y no valida; depósito automático vs manual; BUG-S24-01 packet_084 sin clasificar — condición de desparqueo: recovery se descongela, o D-242/D-243 los sustituye formalmente.
- **P-152** — origen de los 405 valores de `time_scope_raw` sin metadata — condición de desparqueo: que exista una fuente de procedencia por campo (schema con campo de procedencia, P-196) o una re-corrida de Phase 1 que la registre. Razón del parqueo: verificado en S34 y S35 que no es determinable desde el repo — los records no declaran origen de campo.
- **U-1** — `geography_if_explicit` no cruza el puente; `card_record` no tiene campo geográfico — `card_record.schema.json` declara 12 propiedades (`id`, `round`, `observation`, `source`, `date`, `source_type`, `domain`, `actor`, `evidence_base`, `extraction_status`, `entities`, `figures`), ninguna geográfica — condición de desparqueo: cuando Phase 3 produzca TCs sobre el corpus vigente. Razón: los cuatro TCs que la fila invoca no existen en BASE — `output/tension_candidates/` está vacío (solo `.gitkeep`) y el único TC del árbol (`TC-001.md`, referencia) no cita geografía.
- **U-3** — sin campo de fecha de fuente en la Signal Card; `date` de `card_record` se llena desde `time_scope` (`signal_to_markdown.py:19,47,155-161`: prioridad `time_scope_normalized_if_safe` → `time_scope_raw` → `""`), no desde `source_date_if_available` — misma condición y misma razón que U-1.

---

## Conteo

| Grupo | Filas |
|---|---|
| A — pendientes de verificar (Run 2) | 18 |
| B — verificados, esperando decisión | 48 |
| C — decisiones de DSC | 8 |
| D — candidatos a parqueo | 0 |
| **Total abiertos** | **74** |

**Parqueadas (fuera del conteo de abiertos):** 10 (ver `**Parqueados:**` bajo la tabla D).

**Cerrados en esta corrección:** P-145, P-164, P-169 (ver citas bajo la tabla A, justo después de la tabla); P-135, P-129a, P-137, U-2, P-140, P-180 (ver citas en la tabla B, justo después de P-121); P-075, P-077, P-082, P-154, P-102 (ver citas bajo la tabla A, justo después de la tabla); P-143, P-167, P-175, P-190 (pasada S35, ver citas bajo la tabla A, justo después de la tabla); P-176, P-138 (pasada E-S35-6, ver citas en la tabla B, justo después de P-121); P-166 (E-S35-7, diagnóstico corregido, ver cita en la tabla B, justo después de P-138).
**Cerrados en S28, no re-abrir:** P-076, P-107, P-108, P-110, P-112, P-114, P-120, P-122, P-123,
P-124, P-130.
**Cerrados antes:** P-058, P-060, P-063, P-064, P-065, P-066, P-079, P-080, P-101, P-104, P-105,
P-106, P-111, P-113, P-115.
**Huecos de numeración, no son pendientes:** P-024, P-027, P-028, P-051 a P-057, P-081 a P-093
parcialmente, P-098, P-103.

---

## Nota sobre la forma de la cola

48 de 74 ya están verificados y esperan juicio del operador. 18 esperan que alguien mire el repo.
10 más están parqueadas (condición de desparqueo explícita, fuera del conteo de abiertos).
**El cuello es la cola de decisiones, no la de verificación.**

De los 48 del grupo B, cuatro son huecos de puente o de campo (P-134, P-136,
más `local_qualifiers` y `time_scope_raw`) y todos comparten la misma pregunta previa: qué campos
consume Phase 3 realmente. Esa pregunta es el entregable A de Run 3. **Decidir cualquiera de los
cuatro antes de Run 3 repite el error que interrumpió S28 cuatro veces.**

Los cuatro del bloque A (P-119, P-126, P-127, P-128) bloquean los 47 batches y tres de ellos
están explícitamente diferidos al assessment por el handoff de S28.

**La cola creció de 43 a 76 abiertos (+33) por inventario completo de esta pasada, no por trabajo
nuevo pendiente:** BLOQUE II agregó 34 filas nuevas a las tablas abiertas (7 a A, 23 a B, 4 a C,
de las 35 filas P-159 a P-193; P-188 quedó sin agregar por ausencia de confirmación del operador
en el PR), BLOQUE I agregó P-158 a Parqueados y cerró P-145 (sale de A). Neto: A 19→25, B 20→43,
C 4→8, D sin cambio, Parqueadas 5→6. El inventario mide pendientes que ya existían sin fila
propia — la cola no refleja regresión ni trabajo nuevo generado por esta pasada.

**Esta corrección:** el operador ratificó P-188 en la revisión del PR (R4); agregada a Parqueados
sin condición previa. Parqueadas 6→7; abiertos sin cambio (76).

**Pasada posterior a S34-3 (E9/E10):** cerradas P-164 y P-169 (productor identificado para ambos
eslabones); P-168 reformulado (Phase 3 sí corrió, sobre un corpus fuera de `main`); notas de
estado agregadas a P-152, P-159, P-175. BLOQUE II agregó tres filas nuevas a B (P-195, P-196,
P-197); dos filas del encargo original no se agregaron — **DIAGNÓSTICO INVÁLIDO**: P-194 partía de
que `2505539d` no es ancestro de `main`, medido en un clon shallow de la sesión que redactó el
encargo; con `git fetch --unshallow` `2505539d` sí es ancestro de `main`, y el propio historial de
`main` (no solo `preserve/*`) contiene una corrida completa de Phase 3 (Entry Gate → Splitter →
Indexer → 7 scanners → Builder, 23 TCs → Validator, `2505539d..3dfd73ca`) borrada deliberadamente
por `3dfd73ca` ("Clean up test run data"). Reportado al operador, no resuelto aquí — no se agregó
fila nueva por ese hallazgo en esta pasada. P-198 partía de 44 records derivados / 40 `unknown`
para los 35 packets con `metric_type_unclear`; el cruce directo por `source_packet_id` da 54
derivados / 50 `unknown` (las excepciones sí coinciden: 1 `price`, 3 `fee_rate`) — cifras no
verificadas, fila no agregada. Neto: A 25→23, B 43→46, C sin cambio, D sin cambio, Total 76→77.
Parqueadas sin cambio (7). El crecimiento neto (+1, no el +3 estimado en el encargo) es inventario
de hallazgos medidos, no trabajo nuevo.

**Corrección posterior a la pasada S34-3:** el reformulado de P-168 escrito en la pasada anterior
también se apoyaba en la misma medición shallow que invalidó P-194 — afirmaba que el corpus de 75
cards "vive solo en `preserve/*`", lo cual es falso: `2505539d` es ancestro de `main`, y el rango
`2505539d..3dfd73ca` contiene la corrida completa de Phase 3 (Entry Gate → Splitter → Indexer →
7 scanners → Builder, 23 TCs → Validator) antes de que `3dfd73ca` la borrara. Enunciado corregido
en su lugar. Se agrega P-199 (registra el hallazgo de la corrida borrada y su reencuadre del
diagnóstico de S33 sobre reproducibilidad de Phase 3) y P-200 (fija a 54/50 la cifra de
`metric_type_unclear` derivado, sustituyendo el 44/40 que P-198 no sostuvo). Neto: A sin cambio
(23), B 46→48, C sin cambio, D sin cambio, Total 77→79. Parqueadas sin cambio (7). El crecimiento
(+2) es corrección de inventario — un enunciado erróneo introducido por el propio sistema de
registro y el hallazgo que su detección produjo — no trabajo nuevo.

**Nota para el operador — R-G, precondición de plantilla:** P-197 dejó de ser hipótesis: costó una
fila falsa (P-194) y un enunciado contaminado en `main` (P-168, corregido arriba). Antes de
cualquier afirmación basada en historial (`git log`, `--diff-filter`, `merge-base`, ancestría),
ejecutar `git rev-parse --is-shallow-repository`; si es `true`, `git fetch --unshallow origin`
antes de medir. Una consulta histórica sobre clon shallow devuelve el límite del clon con forma
de hallazgo. Aplica también a las sesiones que redactan encargos, que es donde falló esta vez.

**Input correctivo posterior a la pasada S34-3:** cierra P-180 con enunciado corregido antes de
cerrar — el vigente decía "estructuralmente vacías — lector sin escritor"; lo medido fue lo
opuesto: `FROZEN_HEADING_RE` sí matcheó, sobre la propia fila que documentaba el problema, y volcó
filas del ledger dentro de "Superficie congelada" en `state/STATE.md`; resuelto por sello S5,
ejecutado en PR #95. Suma a P-153 la instancia S34 de esa clase (la restricción quedó muda
exactamente donde falló). Delta: B 48→47 (P-180 sale de abiertos a Cerrados), A/C/D y Parqueadas
sin cambio. Total abiertos 79→78. El crecimiento (0 filas nuevas, −1 por cierre) es corrección de
inventario, no trabajo nuevo.

**Encargo E11 — delta pendiente del ledger:** dos ediciones, ninguna reabre una fila cerrada. (1)
Corrige el veredicto de cierre de P-169 (la fila sigue en Cerrados): "productor único" era
incompleto — secuencia real con historia completa es `22823aa6`/`d0109d78` (75 records vía la
skill `p1-extract-records`) → `2c44fa8f` (1,103 vía `bulk_extract.py`) → `3e5a8a27` (revierte los
1,103 por degradación de calidad) → `a6870291..41b83a82` (re-producen por lotes vía la skill,
marcados 'LLM'). El productor del corpus vigente es la skill, no el script; confirma D-179 por el
ángulo inverso. (2) Agrega P-201 a la tabla B: `bulk_extract.py` queda fuera de la ruta de
producción del corpus vigente, lo que pone en cuestión a qué componente le corresponde la rama de
fallo que el CONTRACT de Phase 1b manda implementar (afecta D-242/D-243, A9, A12). Delta: B 47→48,
A/C/D y Parqueadas sin cambio. Total abiertos 78→79. El crecimiento (+1) es la fila que registra el
hallazgo de (1), no trabajo nuevo.

**Pasada S35 (E-S35-2):** 4 cierres ratificados por el operador (1 fusión de duplicado, 3
hecho-respondidos): P-143 fusionada en P-142 (duplicado verbatim); P-167 cerrada (0 ocurrencias de
campos métricos/temporales bajo `phases/03-inventory-mapping/`); P-175 cerrada (unknown 722/1,178
en records, absorbida por P-159); P-190 cerrada (merge `879cc64` confirmado). 2 reformulaciones por
ejecución de PR #97: P-142 (reparación parcial de la rama de fallo de Phase 1b documentada, nueva
pregunta verificable colgada de P-201); P-150 (mecanismo de `infer_actor_level` reparado, residuo en
`pricing_page`). 2 correcciones de enunciado: P-153 (5→7 archivos con serie `Rule 1..N`, causa raíz
R-D citada); P-157 (+1 rama sin registrar, `claude/pricing-page-source-type-k4ycey`). Cero
reaperturas. Delta: A 23→19 (P-143, P-167, P-175, P-190 salen), B y C sin cambio, D sin cambio.
Total abiertos 79→75. Parqueadas sin cambio (7). La reducción es corrección de inventario y cierre
ratificado, no trabajo nuevo.

**Encargo E-S35-6 — cierres, parqueos y correcciones por medición E-S35-5:** el operador ratificó
al lanzar el encargo los cierres y parqueos derivados de E-S35-5 (BASE `a6b8d19d`). 2 cierres por
magnitud cero medida en todas las capas y por ausencia de consumidor (P-191): P-176 (`skeleton_invalid`,
10 declarantes, 0 usos como valor) y P-138 (`uncertainties` sin consumidor en Phase 3 ni en el
puente). 1 edición **DIAGNÓSTICO INVÁLIDO** (R-C): el cierre propuesto de P-166 se apoyaba en "0
ocurrencias de 'comparab' en phases/, .claude/skills/ y docs/"; medido: 4 ocurrencias
(`research_directions_protocol.md` ×3, `signal_extraction_contract.md` ×1). La premisa no se
sostiene contra el árbol; P-166 sigue abierta sin cambio, reportada aparte. 3 parqueos con condición
de desparqueo explícita: P-152 (de A), U-1 y U-3 (de B). 2 filas quedan ABIERTAS pero reducidas por
medición: P-136 (partida en dos casos — (a) resuelto, `author_or_actor_if_available`; (b) abierto,
`snippet_context_before`/`snippet_context_after`, con lector activo en `signal_card_defect_check.py`)
y P-192 (el solapamiento estructural se acota a `comparative_commentary`; `derived_calculation` mide
0 usos en toda capa donde el campo existe). 2 correcciones de enunciado sin cambio de estado: P-157
y P-150, ambas por causa raíz R-D — el enunciado se escribió antes del merge de PR #99 (que borró
`claude/pricing-page-source-type-k4ycey`) y el árbol lo superó. 11 filas con puntero de rama muerta
corregido (P-119, P-126, P-129b, P-132, P-134, P-136, P-147, P-148, P-149, P-150, P-151): seis ramas
verificadas ausentes de origin tras `fetch --prune` (`claude/corpus-measurement-field-mapping-y4rrnl`,
`claude/medicion-time-scope-verbatim-9jp017`, `claude/field-lifecycle-population-audit-a060e7`,
`claude/vocab-schema-measurement-8t49as`, `claude/p150-bulk-extract-vocab-comparison-sealed`,
`claude/pricing-page-source-type-k4ycey`), sustituidas por puntero al artefacto en `main` que ya
las acompañaba en la misma cita; todos los artefactos verificados presentes en BASE antes de la
sustitución. Delta real: A 19→18 (P-152 sale), B 48→44 (P-176 y P-138 cierran, U-1 y U-3 se
parquean — 4, no los 5 que el cierre de P-166 habría producido). Total abiertos 75→70, no el 69
proyectado por el encargo — la diferencia es exactamente P-166, que no cerró por R-C. Parqueadas
7→10 (P-152, U-1, U-3). Control de tokens: ninguna fila abierta cambió su token ·mov (D-256 cl. 3 —
escribir una medición no es movimiento); los tokens de P-176, P-138, U-1 y U-3 salieron con sus
filas.

**Encargo E-S4-S36 — sello S4 aplicado + pasada de ledger:** Parte 1 (lectura de los tres contratos
de validador, `source_intake_validator.md:35-50`, `data_extraction_validator.md:33-47`,
`signal_extraction_validator.md:34-48`): ninguno enuncia la estructura de su propio documento de
salida — solo los enums de estado y check; `data_extraction_validator.md:384` propone un campo
(`rework_attempt_count`) ausente de `data_extraction_validator.schema.json` (`required`, líneas
8-17), reforzando la dependencia. Veredicto 1c = **DEPENDIENTES**. Parte 2, rama MARCAR ejecutada
(no AUTOSUFICIENTES): los 4 schemas (`signal_validation.schema.json`,
`signal_inventory_gate.schema.json`, `source_intake_validation.schema.json`,
`data_extraction_validator.schema.json`) reciben `$comment` como primera propiedad de su objeto
raíz; ninguno se retira; probado JSON válido en los 4 y `vocab_check.py` en exit 0 antes y después,
salida idéntica byte a byte. P-181: Estado actualizado citando esta PR y el veredicto 1c; fila
sigue abierta (marcar no cierra); ·mov sin cambio (S34). P-182: enunciado corregido — no los mismos
4 valores en los 4 schemas (`source_intake_validation.schema.json` declara 5, agrega `parking_lot`,
ya documentado en `state/output/vocab_check_blind_spots.md:225-229`); fila sigue abierta; ·mov sin
cambio (S34). 5 filas nuevas a B (P-202 a P-206), cada una re-medida de forma independiente contra
el árbol antes de escribirse (trazado programático de las 29 Signal Cards vigentes vía extraction
record → skeleton → finding para P-202; verificación directa de campos de schema para P-203;
barrido completo de los 1,178 findings para P-204; grep directo para P-205; lectura de módulo y
schema para P-206) — ninguna se copió sin re-medir de una fuente externa a esta sesión.
**Verificación de conteo:** el conteo real de filas se tomó de `state/scripts/ledger_check.py`
(`get_table`, que reconoce IDs `[PU]-\d+[a-z]?` — incluye sufijos como `P-129b`), no de un script
ad-hoc; un primer intento de recuento manual con una expresión regular más estrecha (sin el sufijo
opcional) contó B=42 y sugirió un desfase preexistente que no existía — **DIAGNÓSTICO INVÁLIDO**,
descartado antes de escribirse en el bloque Conteo; el 43 previamente declarado era correcto. Delta
real: A sin cambio (18), B 43→48 (+5 filas nuevas), C y D sin cambio (8, 0). Total abiertos 69→74.
